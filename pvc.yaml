apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: feedback-app-postgres-pv-claim
  namespace: fbapp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi # Adjust storage size as needed
